# Ethereum-Consensus-Explorer
Interactive block explorer for Ethereum's consensus layer. Fetches real-time beacon chain data (blocks, proposers, attestations, finality) from a public Beacon Node API. Built in vanilla JS, no frameworks.
